# Example on how to use opencv and pixi

- Install `pixi` using
    ```shell
    curl -fsSL https://pixi.sh/install.sh | bash
    ```
    or checkout https://pixi.sh

- Clone this repo

- Then run in the repo:
    ```shell
    pixi run start
    ```